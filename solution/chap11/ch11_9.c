/*
	교재 11장 프로그래밍 과제 9
		배열 A[]에서 탐색하여 배열 요소의 인덱스를 반환하는 함수를 작성
		int search(int *A, int size, int search_value);
*/
#include <stdio.h>

int main(void)
{
	int salary[] = { 50, 100, 70, 30, 75, 200, 150, 130 };
	int no;

	// search함수를 이용해서 결과를 no에 할당
	// insert code here!!

	printf("월급이 200만원인 사람의 인덱스: %d\n", no);

	return 0;
}

int search(int* A, int size, int search_value)
{
}