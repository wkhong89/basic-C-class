/*
	교재 11장 프로그래밍 문제 2
		2개의 정수 합과 차를 동시에 반환하는 함수
*/
#include <stdio.h>

void get_sum_diff(int x, int y, int* p_sum, int* p_diff);

int main(void)
{
	int sum, diff;
	int n1 = 100, n2 = 300;

	// n1, n2의 합과 차를 sum과 diff에 저장하기 위한 get_sum_diff함수 호출
	// insert code here!

	printf("원소들의 합=%d\n", sum);
	printf("원소들의 차=%d\n", diff);

	return 0;
}

void get_sum_diff(int x, int y, int* p_sum, int* p_diff)
{
	// inserted code here!!

}